# 更新历史（Changelog）

本项目所有重要变更都记录在此。项目以持续更新的 debug 构建推进（`versionName` 恒为 `0.1.0`），下列"版本"为**构建里程碑**而非正式发布的版本号递增。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。

## [未发布]

暂无。

## 2026-09 — MiMo 语音双协议接入与图片气泡整合（当前版本）

### 新增
- **MiMo（小米）语音原生协议接入**：小米 MiMo 的 ASR / TTS **复用同一个 `POST /v1/chat/completions`**，并非 OpenAI 的 `/audio/*` 端点。语音客户端改为**双协议自动路由**——host 含 `xiaomimimo.com` 时走 MiMo 原生（ASR 用 `input_audio` 传 `data:audio/wav;base64,…`，TTS 从 `choices[0].message.audio.data` 取 base64），其它网关仍走标准 `/audio/transcriptions` + `/audio/speech`。Base URL 自动归一化（剥离误粘的 `/chat/completions`、`/audio/*` 后缀，缺 `/v1` 时自动补齐）。
- **WAV 录音器**（`WavRecorder`）：`AudioRecord` 采集 16kHz 单声道 PCM 并自补 44 字节 WAV 头（约 32 KB/s）。替换原 MediaRecorder（AAC/m4a），因为 MiMo 语音接口**只接受 wav / mp3**。
- 设置页新增 **ASR 识别语言**选择，以及 **MiMo 音色预设**（如 Chloe 等）。

### 变更
- **语音轮改为延迟落库**：新增 `ChatPhase.SpeakingVoice` 覆盖整个语音轮（请求 → 流式文本 → 语音合成 → 入库），角色**文字与音频在同一次写入中落库**，不再先出字后补语音。
- 语音轮占位气泡改为 **「对方正在讲话」**（三点跳动动画），避免文字先于语音出现造成的出戏感。

### 修复
- **语音识别 http 400**：根因是 URL 被拼成 `…/chat/completions/audio/transcriptions`（协议叠加）+ 录音格式 m4a 不被接受，二者叠加导致。双协议路由 + WAV 录音后解决。
- **图片消息气泡整合**：此前图片以裸图插入聊天流，既无气泡容器也无头像，且与上下气泡无间距。现将**文字 + 图片合并为同一条消息气泡**（4 分支渲染：语音 / 文字+图片 / 纯文字 / 纯图片），带相框、头像、加载转圈 / 失败占位，点击可全屏预览；群聊页同步修复。

## 2026-09 — 语音消息与仓库结构整理

### 新增
- **语音消息（DB v9）**：聊天页长按「按住 说话」录音（上滑取消），录音经 ASR 转写后作为语音消息发出；**角色回复会自动带一条语音**，点气泡即可播放（时长随气泡显示、转写文字以小字附在下方）。TTS / ASR 走你自己配置的 OpenAI 兼容端点（`/v1/audio/speech`、`/v1/audio/transcriptions`），配置逐级回落 **ASR → TTS → 对话**；支持**每个角色单独设音色**（`cards.ttsVoice`），留空则回落设置页全局音色。
- 角色列表卡片菜单新增 **「重新开始聊天」**：清空聊天记录、好感度与共同记忆，保留角色卡设定与状态栏。
- 设置页改造：三组配置改为**可折叠**（对话 / 图像 / 语音，默认收起并显示当前模型），新增语音模型配置段，并补上**返回上一级**按钮。

### 变更
- **移除系统语音播报链路**（旧 `SpeechKit`：系统 TTS 朗读 + 系统 STT 听写）。语音能力全部改为用户配置的云端端点，不再依赖设备语音引擎，也不再使用手机自带播报。

### 构建 / 仓库
- **补齐 Gradle Wrapper**（`gradlew`、`gradlew.bat`、`gradle/wrapper/*`），克隆后可直接 `./gradlew assembleDebug`，无需本机预装 Gradle。
- 补齐开源标准文件：MIT `LICENSE`、`.editorconfig`、`.gitattributes`、`CONTRIBUTING.md`、GitHub Actions CI（单测 + 打包）、Issue / PR 模板。
- 清理仓库根目录 40 个 `build*.log` 构建日志；README 更新项目结构、构建与语音配置说明。

## 2026-09 — 图片体验二改

### 新增
- **一致性出图的"角色视觉档案"**（DB v8，`cards.visualAnchor`）：
  - 某张卡首次出图前，先让文本模型通读整张卡（description / personality / scenario / 属性面板外貌穿着 / 开场白 / 示例），提炼出 ≤200 字**固定中文形象档案**（发色、瞳色、体型、常服、画风），**落库后永久复用**。
  - 之后每张图的提示词都改为中文三段式：`形象档案（固定不变）+ 近期剧情（约 4 轮）+ 这张照片的内容（姿势/表情/镜头）`。
  - 逐级降级：提炼失败 → 属性兜底 → 再失败展示错误，**绝不阻断对话**。
- **"图片接收中"动态气泡**：以拟真的微信风格占位（转圈 + 呼吸闪烁的"图片接收中…"文字 + `n/m` 计数）替代原先生硬的"正在生成照片 n/m"技术提示。

### 修复
- 出图链路中与模型名 / 服务商协议相关的边界问题（详见下方"发照片链路修复"条目的三层根因）。

## 2026-09 — 发照片链路端到端修复

"角色答应发照片但什么都没有出现"的排查结论（经 adb/logcat 逐步定位）实为**三层叠加根因**：

### 修复
1. **请求根本没发出** — `generateImage` 跑在主线程，被 `NetworkOnMainThreadException` 掐断且异常被静默吞掉。现整体包 `withContext(Dispatchers.IO)`。
2. **404 —— 协议不匹配** — 阿里云百炼（DashScope）**没有** OpenAI 兼容的 `/images/generations` 端点。新增 `DashScopeImageClient` 走**原生异步任务协议**（提交 `POST /api/v1/services/aigc/text2image/image-synthesis` → 每 3s 轮询 `GET /api/v1/tasks/{id}` ≤120s → 取结果 URL）。仓库层按 host 自动路由，其它网关仍走标准接口。
3. **400 "url error" —— 模型名不是出图模型** — 文本模型会被出图接口拒绝。若配置的模型被判无效，自动改用官方推荐 `wanx2.1-t2i-turbo` 重试一次（companion 常量 `DASHSCOPE_FALLBACK_MODEL`）。

### 新增
- 设置页「发一张测试图」失败时写 `CharChatNet` 日志，服务端响应原文可直接从 logcat 读取（不再只能看界面提示）。

## 2026-09 — 开场白选择与健壮性批

### 新增
- **开场白选择弹框**（DB v7，`cards.alternateGreetings`）：带 firstMes / SillyTavern `alternate_greetings` 的卡，首次进聊天弹框预览并选择用哪条开场（可跳过）。导入按 id 覆盖，重导不丢历史、老卡也能获得备选开场白。
- **回复语言跟随**：新增 `ReplyLanguage` 规则——回复语言跟随玩家每一轮的输入语言（中文/日文/韩文/英文），提示词中以最高优先级声明，英文源卡不再把对话带跑成英文。
- **图像连接自测**：设置页新增「测试图像接口」（探测 `/models` 并列出出图模型为可点 chip，不耗额度）与「发一张测试图」（真实出图）。

### 修复
- 消息与开场白里的残留垃圾：markdown（`# **…**`、`---`）、自创全角标签（`【SFW/…】`）、代码块——渲染层统一清洗、开场白入库前清洗；全角控制标签白名单放行而非误删；修复 `RoundController` 若干正则 bug（`---{2,}` 应为 `---+`、贪婪 `[^】]*` 改 `[^【】]*`、PHOTO 提取顺序）。
- 中文第三人称旁白残留：提示词加显式反例抑制（无代词开头的中文旁白客户端正则无法可靠拦截）。
- **鉴权加固**（真机 401 后的纵深防御）：`ApiKey.normalize()` 清洗粘贴污染的 Key；`sk-`/`tp-` 与域名混用提示；双鉴权头 `Bearer` + `api-key`；服务端错误原文回显到 UI 与 logcat；保存时统一清洗 URL/Key 以修复历史脏数据。

## 2026-09 — v0.1.0 基线（第一版 APK）

功能完整的初始构建（开发计划 P0–P8）：

### 新增
- 18+ 年龄确认入口、底部导航壳（角色列表 / 群聊 / 设置）。
- SillyTavern / TavernAI 卡导入：PNG V2/V3（`tEXt`）与 JSON 与粘贴；自动抽头像。
- 自动生成的攻略属性（外貌/穿着/性格/位置/在做什么/性癖 + 默认兜底），属性面板可编辑。
- 玩家背景设定（全局默认 + 单卡）。
- 流式 1v1 聊天：单轮 ≤5 气泡、`[[NEXT]]` 切分、长按编辑→截断重生成、独立重摇。
- 玩家输入护栏（剥离第三人称指令与改数值片段，零额外调用）。
- 好感度/关系系统：隐藏信号 `[[AFF:+n]]` / `[[REL:阶段]]` 驱动。
- `[[PHOTO: 画面]]` 出图：`OpenAIImageClient` + 图片气泡。
- 群聊：`@名字` / `@全体`、串行逐角色编排、各自出图与好感推进。
- 语音：麦克风转文字（系统识别）+ TTS 朗读 + 自动朗读开关。
- 共同回忆：`[[MEM:...]]` 落库（封顶/去重）、每轮注入、可查看编辑。
- 世界书支持（空 keys 条目按常驻、触发范围覆盖最近上下文）。
- 备份导出/导入（base64 头像自包含 JSON；MERGE/OVERWRITE 模式）。
- BYOK 设置与连接测试；Key 存 Keystore 加密区；关闭设备云备份（`dataExtractionRules`）。
- 纯函数层 100+ 单元测试。
